pipeline{
    agent any
    environment{
        IMAGE_NAME="hothaifaz11/ado-backend"
    }
    stages{
        stage("build docker image"){
            
            steps{
                script{
                env.IMAGE_TAG = "v${BUILD_NUMBER}-${GIT_COMMIT.take(5)}"
            }
                withCredentials([usernamePassword(credentialsId: 'hodi-docker', passwordVariable: 'docker_pass', usernameVariable: 'docker_user')]) {
                    sh '''
                    docker build -t ${IMAGE_NAME}:$IMAGE_TAG -t ${IMAGE_NAME}:latest .
                    echo ****** login to docker hub ******
                    echo $docker_pass | docker login -u $hodi_docker --password-stdin
                    echo ****** push to docker hub ******

                    docker push ${IMAGE_NAME}:$IMAGE_TAG
                    docker push ${IMAGE_NAME}:latest
                    '''
                }
            }
        }
        
    }
}

